name: Build OpenSSL for Android
env:
  DEFAULT_OPENSSL_BRANCH: 'openssl-3.5'
on:
  workflow_dispatch:  # Manual trigger
    inputs:
      openssl_branch:
        description: 'OpenSSL branch/tag to build'
        required: true
        default: "master"
        type: string
  #schedule:
    #- cron: '0 0 * * 1,5'
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build-openssl-android:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [arm, arm64]
        include:
          - arch: arm
            android_abi: armeabi-v7a
            openssl_target: android-arm
          - arch: arm64
            android_abi: arm64-v8a
            openssl_target: android-arm64
        build_type:
          - Release
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '23'
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential git perl make curl llvm
      
      - name: Download NDK
        run: |
          mkdir -p ${ANDROID_HOME}/ndk
          cd ${ANDROID_HOME}/ndk
          curl -O https://dl.google.com/android/repository/android-ndk-r28-linux.zip
          unzip -q android-ndk-r28-linux.zip
          echo "ANDROID_NDK_HOME=${ANDROID_HOME}/ndk/android-ndk-r28" >> $GITHUB_ENV
          
          # Verify NDK installation
          ls -la ${ANDROID_HOME}/ndk/android-ndk-r28/toolchains/llvm/prebuilt/linux-x86_64/bin/
      
      - name: Clone OpenSSL
        run: |
          git clone https://github.com/openssl/openssl.git
          cd openssl
          # Use the branch specified in workflow_dispatch input or default to master
          BRANCH_TO_USE="${{ github.event.inputs.openssl_branch || env.DEFAULT_OPENSSL_BRANCH }}"
          echo "Checking out OpenSSL branch/tag: $BRANCH_TO_USE"
          git checkout $BRANCH_TO_USE
          
          # Get OpenSSL version and save it
          echo "OPENSSL_BRANCH=$BRANCH_TO_USE" >> $GITHUB_ENV
      
      - name: Set up environment variables
        run: |
          echo "ANDROID_API=35" >> $GITHUB_ENV
          echo "OUTPUT_DIR=$(pwd)/output/${{ matrix.android_abi }}" >> $GITHUB_ENV
          echo "PACKAGE_DIR=$(pwd)/build" >> $GITHUB_ENV
          echo "SNAPSHOT_TAG=daily-snapshot" >> $GITHUB_ENV
      
      - name: Configure OpenSSL for Android ${{ matrix.android_abi }}
        working-directory: openssl
        run: |
          mkdir -p ${{ env.OUTPUT_DIR }}
          
          # Set up NDK environment variables properly
          export ANDROID_NDK_ROOT=$ANDROID_NDK_HOME
          export PATH=$ANDROID_NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64/bin:$PATH
          
          # Use a single, correct Configure command
          ./Configure ${{ matrix.openssl_target }} no-asm no-apps no-tests no-docs no-legacy \
          no-dtls no-capieng no-cmp no-cms no-comp no-ct \
          no-dgram no-dso no-engine no-filenames no-gost \
          no-http no-module no-padlockeng no-sock \
          no-srp no-srtp no-ssl-trace no-ts \
          no-ui-console no-uplink no-ssl3-method no-tls1-method no-tls1_1-method \
          no-dtls1-method no-dtls1_2-method no-argon2 no-bf no-blake2 no-cast no-cmac \
          no-dsa no-idea no-md4 no-mdc2 no-ocb no-rc2 no-rc4 no-rmd160 no-scrypt \
          no-siphash no-siv no-sm2 no-sm3 no-sm4 no-whirlpool \
          no-afalgeng no-acvp-tests no-brotli no-brotli-dynamic no-buildtest-c++ \
          no-crypto-mdebug no-demos no-devcryptoeng no-egd \
          no-external-tests no-fips no-fips-securitychecks no-fuzz-afl no-fuzz-libfuzzer \
          no-h3demo no-jitter no-ktls no-md2 no-msan no-pie no-rc5 no-sctp \
          no-ssl no-static-engine no-tfo no-trace no-ubsan no-unit-test \
          no-winstore no-zlib no-zlib-dynamic no-zstd no-zstd-dynamic \
          no-weak-ssl-ciphers no-psk no-err no-srp no-ec2m no-tls-deprecated-ec \
            --prefix=${{ env.OUTPUT_DIR }} \
            --openssldir=${{ env.OUTPUT_DIR }}
      
      - name: Build OpenSSL
        working-directory: openssl
        run: |
          # Set up NDK environment variables properly
          export ANDROID_NDK_ROOT=$ANDROID_NDK_HOME
          export PATH=$ANDROID_NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64/bin:$PATH
          
          make -j$(nproc)
          make install
          
      - name: Package .so files
        run: |
          mkdir -p ${{ env.PACKAGE_DIR }}
          
          # Create a directory structure that matches what we want in the zip
          TEMP_DIR=$(mktemp -d)
          mkdir -p $TEMP_DIR/lib/${{ matrix.android_abi }}
          
          # Find the libssl and libcrypto .so files
          LIBSSL=$(find ${{ env.OUTPUT_DIR }} -name "libssl*.so" -type f)
          LIBCRYPTO=$(find ${{ env.OUTPUT_DIR }} -name "libcrypto*.so" -type f)

          # Strip the libraries
          llvm-strip --strip-all $LIBSSL
          llvm-strip --strip-all $LIBCRYPTO
          
          # Copy and rename the .so files
          cp $LIBSSL $TEMP_DIR/lib/${{ matrix.android_abi }}/libssl_3.so
          cp $LIBCRYPTO $TEMP_DIR/lib/${{ matrix.android_abi }}/libcrypto_3.so
          
          # Also copy includes for a complete package
          mkdir -p $TEMP_DIR/include
          cp -r ${{ env.OUTPUT_DIR }}/include/* $TEMP_DIR/include/
          
          # Create zip file
          PACKAGE_NAME="openssl-android-${{ matrix.android_abi }}-${{ env.OPENSSL_BRANCH }}"
          cd $TEMP_DIR
          zip -r ${{ env.PACKAGE_DIR }}/$PACKAGE_NAME.zip .
          
          # Also create a tar.gz file as an alternative
          tar -czf ${{ env.PACKAGE_DIR }}/$PACKAGE_NAME.tar.gz .
          
          # List the created packages
          ls -la ${{ env.PACKAGE_DIR }}/
      
      - name: Publish Daily Snapshot Release
        uses: softprops/action-gh-release@v2
        if: ${{ github.ref == 'refs/heads/main' || github.event_name == 'schedule' }}
        with:
          tag_name: ${{ env.SNAPSHOT_TAG }}
          name: "OpenSSL for Android - Daily Build"
          body: |
            Automated daily build of OpenSSL ${{ env.OPENSSL_BRANCH }} for Android

            This build includes binaries for Android (armeabi-v7a, arm64-v8a)
            
            **Note:** This snapshot may be overwritten by newer daily builds.
          files: |
            ${{ env.PACKAGE_DIR }}/openssl-android-${{ matrix.android_abi }}-*.zip
          prerelease: true
          delete_after: "7d"
      
      - name: Publish Tagged Release
        uses: softprops/action-gh-release@v2
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        with:
          name: "OpenSSL for Android ${{ env.OPENSSL_BRANCH }}"
          body: |
            Release build of OpenSSL ${{ env.OPENSSL_BRANCH }} for Android
          files: |
            ${{ env.PACKAGE_DIR }}/openssl-android-${{ matrix.android_abi }}-*.zip
